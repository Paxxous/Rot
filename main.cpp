#include <iostream>

// Include the sdl2 libraries
#include <SDL.h>
#include <SDL_image.h>
#include <SDL_mixer.h>

// My own game libraries
#include "Window.h"
#include "Game.h"

// Just gonna be a simple warmup (also ctrl + j focuses the editor)
int main(int argc, char* args[]) {
  game();
  return 0;
}